using Game.Map.IO;
using UnityEngine;
using UnityGameFramework.Runtime;


// “입력 → 결과”만 만드는 순수 실행 이기때문에 static으로 ㅁ낟므
public static class TileMapInstaller
{
    GameEntry
    //LoadTextMapData loader = new 
    //public static void Install(MapData mapData)
    //{


    //}
}
